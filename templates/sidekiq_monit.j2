check process sidekiq_{{ app_name }} with pidfile /home/{{ user }}/projects/{{ app_name }}/current/tmp/pids/sidekiq.pid
  depends on redis-server
  start program = "/etc/init.d/sidekiq_{{ app_name }} start" with timeout 90 seconds
  stop program = "/etc/init.d/sidekiq_{{ app_name }} stop" with timeout 90 seconds
  #if totalmem is greater than 200 MB for 2 cycles then restart # eating up memory?
  #if 2 restarts within 3 cycles then timeout